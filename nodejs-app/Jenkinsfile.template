pipeline {
  agent any
   options { 
    buildDiscarder(logRotator(numToKeepStr: '2'))
  }
  stages {
  stage('Test') {
      steps {
        container('nodejs') {
          echo 'Hello World!'   
          sh 'java -version'
        }
      }
    }
    }
  }
